import React from "react";
import classes from "./Header.module.css";

const Header = () => {
  return (
    <header className={classes.header}>
      <h2>News about covid in Baltic states</h2>
    </header>
  );
};

export default Header;
